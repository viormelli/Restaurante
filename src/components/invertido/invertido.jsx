export default function Invertido({ titulo, texto, imagem, invertido }) {

    if (invertido % 2 == 0) {
        //para a direita
        return <>
        
        </>
    } else {
        //para a esquerda
        return <>
        
        </>
    }

}